# git archive（リリースzip作成）から開発・検証専用ファイルを除外する。
# 実際のWindows実機での配布・起動には不要なため。
docker/ export-ignore
Dockerfile export-ignore
.dockerignore export-ignore
files/ export-ignore

# start.batはWindowsのcmd.exeで実行するため、LF改行だと解釈に失敗し
# 何も表示されず即座に終了する問題があった（Issue #3）。CRLFを強制する。
*.bat text eol=crlf
